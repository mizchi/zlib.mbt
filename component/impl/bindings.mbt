// Generated by moon-component
// DO NOT EDIT - this file is regenerated

///| Re-export cabi_realloc for wasm linking
pub fn cabi_realloc(
  old_ptr : Int,
  old_size : Int,
  align : Int,
  new_size : Int,
) -> Int {
  @cabi.cabi_realloc(old_ptr, old_size, align, new_size)
}

pub fn wasmExportDeflateCompress(data_ptr : Int, data_len : Int) -> Int {
  let data : Array[Byte] = {
    let arr : Array[Byte] = Array::new(capacity=data_len)
    for i = 0; i < data_len; i = i + 1 {
      let elem_ptr = data_ptr + i * 1
      arr.push(@cabi.cabi_read_u8(elem_ptr))
    }
    arr
  }
  let result = deflate_compress(data)
  // Lower list to linear memory
  let len = result.length()
  let elem_size = 1
  let ptr = @cabi.cabi_realloc(0, 0, 1, len * elem_size)
  for i, elem in result {
    let elem_ptr = ptr + i * elem_size
    @cabi.cabi_write_u8(elem_ptr, elem)
  }
  let retptr = @cabi.cabi_realloc(0, 0, 4, 8)
  @cabi.cabi_write_i32(retptr, ptr)
  @cabi.cabi_write_i32(retptr + 4, len)
  retptr
}

pub fn wasmExportDeflateDecompress(data_ptr : Int, data_len : Int) -> Int {
  let data : Array[Byte] = {
    let arr : Array[Byte] = Array::new(capacity=data_len)
    for i = 0; i < data_len; i = i + 1 {
      let elem_ptr = data_ptr + i * 1
      arr.push(@cabi.cabi_read_u8(elem_ptr))
    }
    arr
  }
  let result = deflate_decompress(data)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 4 + 8)
  match result {
    Ok(val) => {
      @cabi.cabi_write_i32(retptr, 0) // discriminant: Ok
      let len = val.length()
      let elem_size = 1
      let ptr = @cabi.cabi_realloc(0, 0, 1, len * elem_size)
      for i, elem in val {
        let elem_ptr = ptr + i * elem_size
    @cabi.cabi_write_u8(elem_ptr, elem)
      }
      @cabi.cabi_write_i32(retptr + 4, ptr)
      @cabi.cabi_write_i32(retptr + 4 + 4, len)
    }
    Err(e) => {
      @cabi.cabi_write_i32(retptr, 1) // discriminant: Err
      let (str_ptr, str_len) = @cabi.cabi_lower_string(e)
      @cabi.cabi_write_i32(retptr + 4, str_ptr)
      @cabi.cabi_write_i32(retptr + 4 + 4, str_len)
    }
  }
  retptr
}

pub fn wasmExportGzipCompressStored(data_ptr : Int, data_len : Int) -> Int {
  let data : Array[Byte] = {
    let arr : Array[Byte] = Array::new(capacity=data_len)
    for i = 0; i < data_len; i = i + 1 {
      let elem_ptr = data_ptr + i * 1
      arr.push(@cabi.cabi_read_u8(elem_ptr))
    }
    arr
  }
  let result = gzip_compress_stored(data)
  // Lower list to linear memory
  let len = result.length()
  let elem_size = 1
  let ptr = @cabi.cabi_realloc(0, 0, 1, len * elem_size)
  for i, elem in result {
    let elem_ptr = ptr + i * elem_size
    @cabi.cabi_write_u8(elem_ptr, elem)
  }
  let retptr = @cabi.cabi_realloc(0, 0, 4, 8)
  @cabi.cabi_write_i32(retptr, ptr)
  @cabi.cabi_write_i32(retptr + 4, len)
  retptr
}

pub fn wasmExportGzipDecompress(data_ptr : Int, data_len : Int) -> Int {
  let data : Array[Byte] = {
    let arr : Array[Byte] = Array::new(capacity=data_len)
    for i = 0; i < data_len; i = i + 1 {
      let elem_ptr = data_ptr + i * 1
      arr.push(@cabi.cabi_read_u8(elem_ptr))
    }
    arr
  }
  let result = gzip_decompress(data)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 4 + 8)
  match result {
    Ok(val) => {
      @cabi.cabi_write_i32(retptr, 0) // discriminant: Ok
      let len = val.length()
      let elem_size = 1
      let ptr = @cabi.cabi_realloc(0, 0, 1, len * elem_size)
      for i, elem in val {
        let elem_ptr = ptr + i * elem_size
    @cabi.cabi_write_u8(elem_ptr, elem)
      }
      @cabi.cabi_write_i32(retptr + 4, ptr)
      @cabi.cabi_write_i32(retptr + 4 + 4, len)
    }
    Err(e) => {
      @cabi.cabi_write_i32(retptr, 1) // discriminant: Err
      let (str_ptr, str_len) = @cabi.cabi_lower_string(e)
      @cabi.cabi_write_i32(retptr + 4, str_ptr)
      @cabi.cabi_write_i32(retptr + 4 + 4, str_len)
    }
  }
  retptr
}

pub fn wasmExportZlibCompress(data_ptr : Int, data_len : Int) -> Int {
  let data : Array[Byte] = {
    let arr : Array[Byte] = Array::new(capacity=data_len)
    for i = 0; i < data_len; i = i + 1 {
      let elem_ptr = data_ptr + i * 1
      arr.push(@cabi.cabi_read_u8(elem_ptr))
    }
    arr
  }
  let result = zlib_compress(data)
  // Lower list to linear memory
  let len = result.length()
  let elem_size = 1
  let ptr = @cabi.cabi_realloc(0, 0, 1, len * elem_size)
  for i, elem in result {
    let elem_ptr = ptr + i * elem_size
    @cabi.cabi_write_u8(elem_ptr, elem)
  }
  let retptr = @cabi.cabi_realloc(0, 0, 4, 8)
  @cabi.cabi_write_i32(retptr, ptr)
  @cabi.cabi_write_i32(retptr + 4, len)
  retptr
}

pub fn wasmExportZlibDecompress(data_ptr : Int, data_len : Int) -> Int {
  let data : Array[Byte] = {
    let arr : Array[Byte] = Array::new(capacity=data_len)
    for i = 0; i < data_len; i = i + 1 {
      let elem_ptr = data_ptr + i * 1
      arr.push(@cabi.cabi_read_u8(elem_ptr))
    }
    arr
  }
  let result = zlib_decompress(data)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 4 + 8)
  match result {
    Ok(val) => {
      @cabi.cabi_write_i32(retptr, 0) // discriminant: Ok
      let len = val.length()
      let elem_size = 1
      let ptr = @cabi.cabi_realloc(0, 0, 1, len * elem_size)
      for i, elem in val {
        let elem_ptr = ptr + i * elem_size
    @cabi.cabi_write_u8(elem_ptr, elem)
      }
      @cabi.cabi_write_i32(retptr + 4, ptr)
      @cabi.cabi_write_i32(retptr + 4 + 4, len)
    }
    Err(e) => {
      @cabi.cabi_write_i32(retptr, 1) // discriminant: Err
      let (str_ptr, str_len) = @cabi.cabi_lower_string(e)
      @cabi.cabi_write_i32(retptr + 4, str_ptr)
      @cabi.cabi_write_i32(retptr + 4 + 4, str_len)
    }
  }
  retptr
}

///|
fn main {
  ()
}
